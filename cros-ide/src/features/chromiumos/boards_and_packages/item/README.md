# Item

Contains the Item interface and its implementations. Each item corresponds to a
tree item shown on the boards and packages view.

Items form the following hierarchy. The value in parens indicates the context value assigned to the viewItem.

- root
  - board ('board')
    - package category
      - package name ('package')
